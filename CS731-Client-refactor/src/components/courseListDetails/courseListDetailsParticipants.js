import React from 'react';
import { Box, Typography } from '@mui/material';

const ParticipantsDetail = () => (
  <Box mt={3}>
    <Typography variant="h6">Participants' Detail will be here.</Typography>
  </Box>
);

export default ParticipantsDetail;
